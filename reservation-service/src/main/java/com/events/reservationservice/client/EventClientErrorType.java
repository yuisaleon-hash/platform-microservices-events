package com.events.reservationservice.client;

public enum EventClientErrorType {
    EVENT_NOT_FOUND,
    EVENT_NOT_ACTIVE,
    INSUFFICIENT_CAPACITY,
    EVENT_SERVICE_UNAVAILABLE,
    UNEXPECTED_ERROR
}
